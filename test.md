This is a test file that we will push to git

First we created the project on our local machine by using git clone.
The link for this can be found in github in the repository.

Then we checked on what branch we are working by using git status

Then we changed it to a new branch by using git checkout -b test_branch

This file is an addition to what was already in the main branch.

Now we will first commit it to this branch, push it & then merge it with the main branch.